

def find_numbers(string):
    numstring = ""

    for ch in string:
        number
        try:
            numstring += ch
            number = w2n.word_to_num(numstring)
        except UnboundLocalError:
            continue

    return number